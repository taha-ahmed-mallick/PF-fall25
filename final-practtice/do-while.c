#include <stdio.h>

int main()
{
    int x = 1;
    do
    {
        printf("%d ", x);
        x++;
    } while (x != 5);
    for (int i = 0, y; i <0|| y; i++) {
        int y = 7;
        y--;
        printf("\n\t%d%d", i, y);
    }
    return 0;
}